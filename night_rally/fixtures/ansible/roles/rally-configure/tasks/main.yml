---
- name: install Rally config file
  become: '{{ not local_setup | default("false") | bool }}'
  template:
    src: rally.ini.j2
    dest: "{{ rally_homedir }}/.rally/rally-{{ rally_environment }}.ini"
