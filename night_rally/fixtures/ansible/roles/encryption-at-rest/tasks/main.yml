---

- set_fact:
    system_tasks: "{{ lookup('first_found', params) }}"
  vars:
    params:
      files:
        - "{{ ansible_system | lower }}/main.yml"
        - unsupported.yml

- block:
    - include_tasks: "{{ system_tasks }}"
  become: "{{ ansible_system | lower != 'win32nt' }}"
