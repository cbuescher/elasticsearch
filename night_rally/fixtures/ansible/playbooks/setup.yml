---

- name: run fixtures on target hosts
# ==================================
  hosts: target-hosts
  gather_facts: true
  roles:
    - { role: trim, tags: trim }
    - { role: initialize-data-disk, tags: initialize-data-disk }
    - { role: encryption-at-rest, tags: encryption-at-rest }
    - { role: drop-caches, tags: drop-caches }
